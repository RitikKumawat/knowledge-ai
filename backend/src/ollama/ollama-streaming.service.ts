import { Injectable } from '@nestjs/common';

@Injectable()
export class OllamaStreamingService {
  // TODO(OLLAMA-2): Add streaming answer generation in a future ticket.
  // Intentionally left blank so no streaming API is exposed yet.
}
